---
driver:
  name: vagrant

provisioner:
  name: shell

platforms:
  - name: nomadclient
    driver:
      box: nomadclient
      provider: virtualbox

verifier:
  name: inspec

suites:
  - name: default
    verifier:
      inspec_tests:
        - test/integration/default
